# TODO — MixBrain

Pendências abertas, mais recentes no topo. Formato: data, descrição, contexto.

## 2026-08-23

- [ ] **Exibição de tracks em formato lista.** Trocar a exibição de tracks
  (biblioteca e/ou tracklist do projeto — confirmar escopo exato com o
  Ricardo) e o % de match das tracks para formato de lista, em vez do
  layout atual em cards. Precisa decidir: aplica na biblioteca
  (`tracks-library.tsx`), na tracklist do projeto (`page.tsx`), ou nas
  duas? E o que fica visível por linha (título, artista, BPM, key,
  energia, % match) sem virar tabela ilegível no mobile.
